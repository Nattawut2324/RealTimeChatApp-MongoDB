<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= tls('regis_title') %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <style>
        .con{
            width: 40%;
        }
        @media screen and (max-width: 1200px) {
            .con{
                width: 60%;
            }
        }
        @media screen and (max-width: 768px) {
            .con{
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="con card mt-5 mx-auto">
            <div class="card-header text-center"><h5><%= tls('regis_title') %></h5></div>
            <div class="card-body">
                <form action="/auth/register" method="post">
                    <label for="name"><%= tls('regis_name') %></label>
                    <input autofocus class="form-control mb-1 <%= (locals.register_Error && locals.register_Error.name) ? 'is-invalid' : '' %>" type="text" id="name" name="name" value="<%= (locals.old_data) ? locals.old_data.name : '' %>" autocomplete="off" autocapitalize="none">
                    <% if (locals.register_Error) { %>
                        <div class="text-danger"><%= locals.register_Error.name %></div>
                    <% } %>
                    <label for="username"><%= tls('regis_username') %></label>
                    <input class="form-control mb-1 <%= (locals.register_Error && locals.register_Error.username) ? 'is-invalid' : '' %>" type="text" id="username" name="username" value="<%= (locals.old_data) ? locals.old_data.username : '' %>" autocomplete="off" autocapitalize="none">
                    <% if (locals.register_Error) { %>
                        <div class="text-danger"><%= locals.register_Error.username %></div>
                    <% } %>
                    <label for="password"><%= tls('regis_password') %></label>
                    <input class="form-control mb-1 <%= (locals.register_Error && locals.register_Error.password) ? 'is-invalid' : '' %>" type="password" id="password" name="password" value="<%= (locals.old_data) ? locals.old_data.password : '' %>" autocapitalize="none">
                    <% if (locals.register_Error) { %>
                        <div class="text-danger"><%= locals.register_Error.password %></div>
                    <% } %>
                    <div class="d-flex justify-content-end w-100">
                        <button type="submit" class="btn btn-primary"><%= tls('regis_btn') %></button>
                    </div>
                </form>
                <div class="text-center mt-3">
                    <span><%= tls('regis_alreadyHaveAccount') %> </span><a href="/auth/login"><%= tls('regis_gotoLogin') %></a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>