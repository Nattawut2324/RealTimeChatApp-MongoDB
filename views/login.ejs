<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= tls('login_title') %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <style>
        .con{
            width: 40%;
        }
        @media screen and (max-width: 1200px) {
            .con{
                width: 50%;
            }
        }
        @media screen and (max-width: 768px) {
            .con{
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="con card mt-5 mx-auto">
            <div class="card-header"><h5><%= tls('login_title') %></h5></div>
            <div class="card-body">
                <form class="mx-3" action="/auth/login" method="post">
                    <input autofocus class="form-control mb-3" type="text" placeholder="<%= tls('login_username') %>" id="username" name="username" value="<%= locals.old_username %>" autocapitalize="none">
                    <input class="form-control mb-3" type="password" placeholder="<%= tls('login_password') %>" id="password" name="password" autocapitalize="none">
                    <% if (locals.login_Errors) { %>
                        <div class="alert alert-danger"><%= locals.login_Errors %></div>
                    <% } %>
                    <div class="d-flex justify-content-end w-100">
                        <button type="submit" class="btn btn-primary"><%= tls('login_btn') %></button>
                    </div>

                </form>
                <div class="text-center mt-3">
                    <span><%= tls('login_doNotHaveAccount') %> </span><a href="/auth/register"><%= tls('login_gotoRegister') %></a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>